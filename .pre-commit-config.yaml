repos:
  # Keep in lockstep with the ruff version in uv.lock (grouped by renovate.json).
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.20
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.38.0
    hooks:
      - id: yamllint
        args: [-d, "{extends: relaxed, rules: {line-length: {max: 140}}}"]
        files: ^(packages|examples)/
  - repo: local
    hooks:
      - id: mypy
        name: mypy --strict (mbt-core + adapter-base)
        entry: uv run mypy packages/mbt-core/src packages/mbt-adapter-base/src
        language: system
        pass_filenames: false
        files: '\.py$'

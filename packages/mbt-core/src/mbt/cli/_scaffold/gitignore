# mbt artifacts
target/
mlruns/
mlflow.db
data/
predictions/

# fetched prod-state baseline (lives on the mbt-state branch)
state/

# local environment config (may hold secrets; lives in ~/.mbt by default)
profiles.yml

# Python
__pycache__/
*.py[cod]
.venv/

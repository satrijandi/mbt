# The deployable unit, pinned by digest. IMAGE is written by CI
# (prod-build's deploy-digest step); NETWORK and WORKSPACE are session
# wiring written by ci_bootstrap.py. The Airflow DAGs read this file at
# parse time - changing IMAGE here (or `git revert`) IS the deploy.
IMAGE=
NETWORK=mbt-showcase_default
WORKSPACE=

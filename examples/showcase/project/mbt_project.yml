name: churn_lake
version: "0.1.0"

vars:
  # Meaningfully above the ~0.10 random-baseline PR-AUC of the panel's ~10%
  # churn rate: a gate below the base rate never gates.
  pr_auc_floor: 0.2

model_defaults:
  adapter: xgboost
